<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<html>
<head>
    <title>Pizza Store</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 90%;
            margin: auto;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        h2 {
            margin-top: 30px;
            color: #222;
        }
        .card {
            background: #fff;
            padding: 15px;
            margin: 10px 0;
            border-radius: 8px;
            box-shadow: 0 2px 6px rgba(0,0,0,0.1);
        }
        .card h4 {
            margin: 0;
            font-weight: bold;
        }
        .card p {
            margin: 5px 0 0;
            color: #555;
        }
        .no-items {
            color: #888;
            margin: 10px 0;
            font-style: italic;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>🍕 Our Menu</h1>

    <c:forEach var="cat" items="${categories}">
        <h2>${cat['name']}</h2>

        <c:set var="hasItems" value="false"/>
        <c:forEach var="item" items="${items}">
            <c:if test="${item['categoryId'].toString() eq cat['id'].toString()}">
                <c:set var="hasItems" value="true"/>
                <div class="card">
                    <h4>${item['name']}</h4>
                    <p>Price: ₹${item['price']}</p>
                </div>
            </c:if>
        </c:forEach>

        <c:if test="${!hasItems}">
            <p class="no-items">No items available in this category</p>
        </c:if>
    </c:forEach>
</div>
</body>
</html>
